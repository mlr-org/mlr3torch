% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PipeOpTorchMultiheadAttention.R
\name{nn_attention}
\alias{nn_attention}
\title{Multi-Head Attention}
\usage{
nn_attention(
  embed_dim,
  num_heads,
  dropout = 0,
  bias = TRUE,
  add_bias_kv = FALSE,
  add_zero_attn = FALSE,
  kdim = NULL,
  vdim = NULL,
  batch_first = FALSE,
  need_weights = FALSE,
  avg_weights = TRUE
)
}
\arguments{
\item{embed_dim}{(\code{integer(1)})\cr
Total dimension of the model, i.e. the size of the last dimension of the query tensor.}

\item{num_heads}{(\code{integer(1)})\cr
Number of parallel attention heads. \code{embed_dim} must be divisible by \code{num_heads}.}

\item{dropout}{(\code{numeric(1)})\cr
Dropout probability on the attention weights. Default is \code{0}.}

\item{bias}{(\code{logical(1)})\cr
Whether to add a bias to the input and output projections. Default is \code{TRUE}.}

\item{add_bias_kv}{(\code{logical(1)})\cr
Whether to add a bias to the key and value sequences at dimension 1. Default is \code{FALSE}.}

\item{add_zero_attn}{(\code{logical(1)})\cr
Whether to add a new batch of zeros to the key and value sequences at dimension 1.
Default is \code{FALSE}.}

\item{kdim}{(\code{integer(1)})\cr
Total number of features for the keys. Default is \code{NULL}, which means \code{embed_dim}.}

\item{vdim}{(\code{integer(1)})\cr
Total number of features for the values. Default is \code{NULL}, which means \code{embed_dim}.}

\item{batch_first}{(\code{logical(1)})\cr
Whether the input and output tensors are provided as \verb{(batch, sequence, feature)} (\code{TRUE}) or
as \verb{(sequence, batch, feature)} (\code{FALSE}). Default is \code{FALSE}, as in \code{torch}.}

\item{need_weights}{(\code{logical(1)})\cr
Whether the attention weights are returned in addition to the attention output.
Default is \code{FALSE}.}

\item{avg_weights}{(\code{logical(1)})\cr
Whether the returned attention weights are averaged over the attention heads.
Default is \code{TRUE}. Only has an effect when \code{need_weights} is \code{TRUE}.\cr
Note that \code{\link[torch:nn_multihead_attention]{torch::nn_multihead_attention()}} silently ignores this argument (and behaves as if
it were \code{TRUE}) whenever \code{kdim} or \code{vdim} differ from \code{embed_dim}.}
}
\description{
Multi-head attention as described in \emph{Attention Is All You Need}.

This is a thin wrapper around \code{\link[torch:nn_multihead_attention]{torch::nn_multihead_attention()}} that makes it usable as a
building block of a \code{\link[mlr3pipelines:Graph]{Graph}} of tensor operations:
\enumerate{
\item The \code{forward()} method accepts between one and three tensors, so that both self-attention and
cross-attention can be expressed.
\item The \code{forward()} method returns a bare tensor when \code{need_weights} is \code{FALSE} and a named
\code{list()} with elements \code{"output"} and \code{"weights"} when \code{need_weights} is \code{TRUE}.
}

The \code{forward()} method accepts between one and three tensors:
\itemize{
\item one tensor \verb{(query)}: self-attention, i.e. the tensor is used as query, key and value.
\item two tensors \verb{(query, key_value)}: cross-attention, where the second tensor is used as both key
and value.
\item three tensors \verb{(query, key, value)}: cross-attention with separate key and value tensors.
}
}
\references{
Vaswani, Ashish, Shazeer, Noam, Parmar, Niki, Uszkoreit, Jakob, Jones, Llion, Gomez, N A, Kaiser, Łukasz, Polosukhin, Illia (2017).
\dQuote{Attention is all you need.}
\emph{Advances in neural information processing systems}, \bold{30}.
}
