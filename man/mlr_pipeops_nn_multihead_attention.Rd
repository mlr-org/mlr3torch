% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PipeOpTorchMultiheadAttention.R
\name{mlr_pipeops_nn_multihead_attention}
\alias{mlr_pipeops_nn_multihead_attention}
\alias{PipeOpTorchMultiheadAttention}
\title{Multi-Head Attention}
\description{
Multi-head attention as described in \emph{Attention Is All You Need}.

This is a thin wrapper around \code{\link[torch:nn_multihead_attention]{torch::nn_multihead_attention()}} that makes it usable as a
building block of a \code{\link[mlr3pipelines:Graph]{Graph}} of tensor operations:
\enumerate{
\item The \code{forward()} method accepts between one and three tensors, so that both self-attention and
cross-attention can be expressed.
\item The \code{forward()} method returns a bare tensor when \code{need_weights} is \code{FALSE} and a named
\code{list()} with elements \code{"output"} and \code{"weights"} when \code{need_weights} is \code{TRUE}.
}

The \code{forward()} method accepts between one and three tensors:
\itemize{
\item one tensor \verb{(query)}: self-attention, i.e. the tensor is used as query, key and value.
\item two tensors \verb{(query, key_value)}: cross-attention, where the second tensor is used as both key
and value.
\item three tensors \verb{(query, key, value)}: cross-attention with separate key and value tensors.
}
}
\section{nn_module}{

Calls \code{\link[=nn_attention]{nn_attention()}} when trained, where the parameters \code{embed_dim}, \code{kdim} and \code{vdim} are
inferred as the last dimension of the query, key and value tensors respectively.
}

\section{Parameters}{

\itemize{
\item \code{num_heads} :: \code{integer(1)}\cr
Number of parallel attention heads. The embedding dimension must be divisible by \code{num_heads}.
\item \code{dropout} :: \code{numeric(1)}\cr
Dropout probability on the attention weights.
Default is \code{0}.
\item \code{bias} :: \code{logical(1)}\cr
Whether to add a bias to the input and output projections.
Default is \code{TRUE}.
\item \code{add_bias_kv} :: \code{logical(1)}\cr
Whether to add a bias to the key and value sequences at dimension 1.
Default is \code{FALSE}.
\item \code{add_zero_attn} :: \code{logical(1)}\cr
Whether to add a new batch of zeros to the key and value sequences at dimension 1.
Default is \code{FALSE}.
\item \code{batch_first} :: \code{logical(1)}\cr
Whether the input and output tensors are provided as \verb{(batch, sequence, feature)} (\code{TRUE}) or
as \verb{(sequence, batch, feature)} (\code{FALSE}).
Default is \code{FALSE}, as in \code{torch}.
\item \code{avg_weights} :: \code{logical(1)}\cr
Whether the returned attention weights are averaged over the attention heads.
Default is \code{TRUE}. Only has an effect when the construction argument \code{outnum} is 2.
}

Note that \code{embed_dim}, \code{kdim} and \code{vdim} are \emph{not} parameters, as they are inferred from the
shapes of the input tensors.
}

\section{Input and Output Channels}{

The number of input channels is determined by the construction argument \code{innum}:
\itemize{
\item \code{innum = 1} (default): one input channel \code{"input"}, which is used as query, key and value,
i.e. the \code{PipeOp} performs \emph{self-attention}.
\item \code{innum = 2}: input channels \code{"query"} and \code{"key_value"}, i.e. the \code{PipeOp} performs
\emph{cross-attention}, where the second input is used as both key and value.
\item \code{innum = 3}: input channels \code{"query"}, \code{"key"} and \code{"value"}, i.e. the \code{PipeOp} performs
\emph{cross-attention} with separate key and value inputs.
}

The number of output channels is determined by the construction argument \code{outnum}:
\itemize{
\item \code{outnum = 1} (default): one output channel \code{"output"}, containing the attention output.
\item \code{outnum = 2}: output channels \code{"output"} and \code{"weights"}, where the latter contains the
attention weights.
}

For an explanation see \code{\link{PipeOpTorch}}.
}

\section{Internals}{

All input tensors must be three-dimensional. Depending on the parameter \code{batch_first}, they are
interpreted as \verb{(batch, sequence, feature)} or as \verb{(sequence, batch, feature)}.
The feature dimension is the last dimension in both layouts.

The shape of the attention output is identical to the shape of the query tensor (in both
layouts). The attention weights, on the other hand, are \strong{always} batch-first, irrespective of
\code{batch_first}, because \code{\link[torch:nnf_multi_head_attention_forward]{torch::nnf_multi_head_attention_forward()}} transposes its inputs to a
sequence-first layout before extracting the batch size. Their shape is
\verb{(batch, query_sequence, key_sequence)} if the weights are averaged over the heads and
\verb{(batch, num_heads, query_sequence, key_sequence)} otherwise, where \code{key_sequence} is increased
by 1 for each of \code{add_bias_kv} and \code{add_zero_attn}.

Because \code{\link[torch:nn_multihead_attention]{torch::nn_multihead_attention()}} does not forward \code{avg_weights} when \code{kdim} or \code{vdim}
differ from \code{embed_dim}, the weights are averaged in that case even if \code{avg_weights} is \code{FALSE}.
The shape inference accounts for this.
}

\section{State}{

The state is the value calculated by the public method \verb{$shapes_out()}.
}

\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
# Construct the PipeOp
pipeop = po("nn_multihead_attention", num_heads = 4)
pipeop
# The available parameters
pipeop$param_set
\dontshow{\}) # examplesIf}
}
\concept{PipeOps}
\section{Super class}{
\code{\link[mlr3pipelines:PipeOp]{mlr3pipelines::PipeOp}} -> \code{\link[mlr3torch:PipeOpTorch]{mlr3torch::PipeOpTorch}} -> \code{PipeOpTorchMultiheadAttention}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-PipeOpTorchMultiheadAttention-new}{\code{PipeOpTorchMultiheadAttention$new()}}
\item \href{#method-PipeOpTorchMultiheadAttention-clone}{\code{PipeOpTorchMultiheadAttention$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PipeOpTorchMultiheadAttention-new"></a>}}
\if{latex}{\out{\hypertarget{method-PipeOpTorchMultiheadAttention-new}{}}}
\subsection{Method \code{new()}}{
Creates a new instance of this \link[R6:R6Class]{R6} class.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{PipeOpTorchMultiheadAttention$new(
  id = "nn_multihead_attention",
  innum = 1,
  outnum = 1,
  param_vals = list()
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{id}}{(\code{character(1)})\cr
Identifier of the resulting  object.}

\item{\code{innum}}{(\code{integer(1)})\cr
The number of input channels, which must be between 1 and 3.
This is a \emph{construction} argument (and not a hyperparameter), because it determines the
structure of the \code{\link[mlr3pipelines:Graph]{Graph}}.
The default is 1, which means that the \code{PipeOp} performs self-attention.
See section \emph{Input and Output Channels} for more information.}

\item{\code{outnum}}{(\code{integer(1)})\cr
The number of output channels, which must be 1 or 2.
This is a \emph{construction} argument (and not a hyperparameter), because it determines the
structure of the \code{\link[mlr3pipelines:Graph]{Graph}}.
The default is 1, which means that only the attention output is returned.
See section \emph{Input and Output Channels} for more information.}

\item{\code{param_vals}}{(\code{list()})\cr
List of hyperparameter settings, overwriting the hyperparameter settings that would
otherwise be set during construction.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PipeOpTorchMultiheadAttention-clone"></a>}}
\if{latex}{\out{\hypertarget{method-PipeOpTorchMultiheadAttention-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{PipeOpTorchMultiheadAttention$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
