% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictionTorch.R
\name{as_prediction_data_torch}
\alias{as_prediction_data_torch}
\title{Prediction Data of a Torch Learner}
\usage{
as_prediction_data_torch(x, task, row_ids = task$row_ids, check = TRUE)
}
\arguments{
\item{x}{(\code{prediction_torch})\cr
What \code{.predict()} returned, i.e. a named \code{list()} of prediction types.}

\item{task}{(\code{\link[mlr3:Task]{Task}})\cr
The task that was predicted on.}

\item{row_ids}{(\code{integer()})\cr
The predicted rows.}

\item{check}{(\code{logical(1)})\cr
Whether to check the assembled prediction data.}

\item{...}{(any)\cr
Passed on.}
}
\value{
\code{\link[mlr3:PredictionData]{PredictionData}}
}
\description{
\code{mlr3} copies \code{task$truth()} into the prediction data only for a
\code{\link[mlr3:TaskSupervised]{TaskSupervised}}, and a \code{\link{TaskTorch}} is not one -- it may have any
number of target columns, including none.
The private \code{.predict()} method of \code{\link{LearnerTorch}} therefore gives what it returns this class,
so that this method runs instead of the one for a plain \code{list()} and adds the ground truth.
}
