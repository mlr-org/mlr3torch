% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerTorchTabTransformer.R
\name{nn_tab_transformer}
\alias{nn_tab_transformer}
\title{TabTransformer Network}
\usage{
nn_tab_transformer(
  task = NULL,
  cardinalities = NULL,
  n_features_num = NULL,
  d_out = NULL,
  d_token = 32L,
  depth = 6L,
  n_heads = 8L,
  dim_head = 16L,
  attn_dropout = 0,
  ff_dropout = 0,
  mlp_hidden_mults = c(4L, 2L),
  use_shared_categ_embed = TRUE,
  shared_categ_dim_divisor = 8
)
}
\arguments{
\item{task}{(\code{\link[mlr3:Task]{Task}} or \code{NULL})\cr
The task for which to construct the network. If provided, \code{cardinalities}, \code{n_features_num}
and \code{d_out} are inferred from it and must not be given.}

\item{cardinalities}{(\code{integer()})\cr
The number of levels of each categorical feature. Must be in the same order as the columns of
\code{x_cat}. Only needed if \code{task} is \code{NULL}.}

\item{n_features_num}{(\code{integer(1)})\cr
The number of numeric features, i.e. the number of columns of \code{x_num}.
Only needed if \code{task} is \code{NULL}.}

\item{d_out}{(\code{integer(1)})\cr
The output dimension of the network. Only needed if \code{task} is \code{NULL}.}

\item{d_token}{(\code{integer(1)})\cr
The dimension of the categorical embeddings, i.e. the width of the transformer.}

\item{depth}{(\code{integer(1)})\cr
The number of transformer blocks.}

\item{n_heads}{(\code{integer(1)})\cr
The number of attention heads.}

\item{dim_head}{(\code{integer(1)})\cr
The dimension of each attention head.}

\item{attn_dropout}{(\code{numeric(1)})\cr
The dropout probability applied to the attention weights.}

\item{ff_dropout}{(\code{numeric(1)})\cr
The dropout probability applied inside the feed-forward blocks.}

\item{mlp_hidden_mults}{(\code{integer()})\cr
Multipliers defining the hidden widths of the output MLP, relative to the size of its input.}

\item{use_shared_categ_embed}{(\code{logical(1)})\cr
Whether to reserve part of the embedding dimension for a per-column embedding that is shared
by all levels of that column.}

\item{shared_categ_dim_divisor}{(\code{numeric(1)})\cr
The shared column embedding uses \code{d_token \%/\% shared_categ_dim_divisor} dimensions.
Must be at least \code{2}, so that at most half of the token is the shared embedding.}
}
\description{
Implements the TabTransformer architecture of Huang et al. (2020).

The categorical features are embedded into a \code{d_token}-dimensional space (optionally reserving
\code{d_token \%/\% shared_categ_dim_divisor} dimensions for a per-column \emph{shared} embedding) and are
then contextualized by a stack of \code{depth} transformer blocks. The resulting contextual
embeddings of shape \verb{(batch, n_categorical, d_token)} are flattened and concatenated with the
layer-normalized numeric features. An MLP with hidden widths
\code{mlp_hidden_mults * (d_token * n_categorical + n_numeric)} maps this to the output.

Only the categorical features pass through the transformer; the numeric features bypass it.
}
\section{Input and Output}{

The \code{forward()} method takes the arguments \code{x_num} (a \code{float} tensor of shape
\verb{(batch, n_features_num)}) and \code{x_cat} (a \code{long} tensor of shape
\verb{(batch, length(cardinalities))}), both of which default to \code{NULL}.
Categorical codes are expected to be 1-based, as produced by \code{\link[=batchgetter_categ]{batchgetter_categ()}}.
The output has shape \verb{(batch, d_out)}.
}

\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
network = nn_tab_transformer(
  cardinalities = c(3, 4), n_features_num = 5, d_out = 2,
  d_token = 8, depth = 1, n_heads = 2
)
x_num = torch::torch_randn(2, 5)
x_cat = torch::torch_cat(list(
  torch::torch_randint(1, 3, c(2, 1), dtype = torch::torch_long()),
  torch::torch_randint(1, 4, c(2, 1), dtype = torch::torch_long())
), dim = 2)
network(x_num = x_num, x_cat = x_cat)
\dontshow{\}) # examplesIf}
}
\references{
Huang X, Khetan A, Cvitkovic M, Karnin Z (2020).
\dQuote{TabTransformer: Tabular Data Modeling Using Contextual Embeddings.}
2012.06678, \url{https://arxiv.org/abs/2012.06678}.

Shazeer N (2020).
\dQuote{GLU Variants Improve Transformer.}
2002.05202, \url{https://arxiv.org/abs/2002.05202}.
}
