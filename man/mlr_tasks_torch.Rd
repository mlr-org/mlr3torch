% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TaskTorch.R
\name{mlr_tasks_torch}
\alias{mlr_tasks_torch}
\alias{TaskTorch}
\title{Generic Torch Task}
\description{
A general-purpose \code{\link[mlr3:Task]{Task}} for learning problems that are neither classification nor
regression, such as multi-label classification, multi-output regression or autoencoders.
It may have any number of target columns, including none at all, so it is supervised or
unsupervised depending only on whether target columns were given.
Nothing beyond that is assumed about the structure of the problem.

Adding a proper task type to \code{mlr3} requires a \code{Task} class, a \code{Prediction} class, a handful of
\code{PredictionData} methods, a \code{Measure} and a number of entries in
\code{\link[mlr3:mlr_reflections]{mlr_reflections}}; the \emph{Adding a Custom Task Type} vignette walks through
this.
\code{TaskTorch} is the quick alternative: the task type \code{"torch"} is registered once by \code{mlr3torch}
and every custom problem is expressed as an \emph{instance} of \code{TaskTorch} rather than as a new class.
What would otherwise be S3 methods dispatching on the task class are fields of the task instead,
and they are inferred from the types of the target columns when they are not given.

The price is that all \code{TaskTorch} instances share a single task type, so \code{mlr3} cannot tell a
multi-label task apart from a multi-output regression task.
Learners and measures for different problems are therefore interchangeable as far as \code{mlr3} is
concerned, and mixing them up produces an error somewhere in \code{torch} instead of an error from
\code{mlr3}.
Use \code{TaskTorch} for experiments and one-off problems, and add a real task type when you package
up a learning problem for others.
}
\section{Inference}{
 The three things \code{mlr3torch} needs to know about a task are the target tensor
\code{y} of a batch (\code{\link[=get_target_batchgetter]{get_target_batchgetter()}}), the number of output units of the network
(\code{\link[=output_dim_for]{output_dim_for()}}) and how a network output becomes a prediction
(\code{\link[=encode_prediction]{encode_prediction()}}).
All three are derived from the target columns:\tabular{lllll}{
   target columns \tab \code{y} \tab \code{output_dim} \tab \code{response} \tab \code{prob} \cr
   one \code{factor} / \code{ordered} with \code{k} levels \tab \code{long} codes \tab \code{k} \tab \code{factor()} \tab \code{matrix()}, \code{k} columns \cr
   one \code{numeric} / \code{integer} \tab \code{float} \verb{(n, 1)} \tab 1 \tab \code{numeric()} \tab -- \cr
   \code{d} \code{numeric} / \code{integer} \tab \code{float} \verb{(n, d)} \tab \code{d} \tab \code{matrix()}, \code{d} columns \tab -- \cr
   one \code{logical} \tab \code{float} \verb{(n, 1)} \tab 1 \tab \code{logical()} \tab \code{numeric()} \cr
   \code{d} \code{logical} \tab \code{float} \verb{(n, d)} \tab \code{d} \tab \code{matrix()}, \code{d} columns \tab \code{matrix()}, \code{d} columns \cr
   none \tab -- (no \code{y} in the batch) \tab -- \tab -- \tab -- \cr
}


Note that a two-level \code{factor} target is \emph{not} treated as binary classification: it gets two
output units and is trained with a cross-entropy loss, like any other \code{factor} target.
Encode the target as \code{logical} to get the single-logit variant.

Any of the three can be overwritten by passing \code{target_batchgetter}, \code{output_dim} or
\code{prediction_encoder} to the constructor, which is also the only way to use a combination of
target columns that is not in the table above.

What the task specifies is the \emph{default}, and a learner has the last word on two of the three:
\code{\link{LearnerTorchModule}} (\code{lrn("torch.module")}) takes a \code{target_batchgetter} of its own, and any
\code{\link{LearnerTorch}} can overwrite the private \verb{$.encode_prediction()} method.
This matters when the network and the loss expect a different encoding than the task's default,
e.g. when training on one-hot encoded class labels.
}

\section{Tasks without a Target}{
 A task with no target columns is unsupervised, and there is
nothing to infer from, so \code{output_dim} and \code{prediction_encoder} have to be given.
Its batches have no \code{y} element, so the loss is called as \code{loss(y_hat, NULL)} and has to ignore
its second argument.

If the target of a batch is a function of its \emph{input} rather than of a column -- an autoencoder
reconstructing its input, a denoising or masked objective, contrastive pretraining -- pass a
\code{target_batchgetter} that declares an \code{x} argument, which receives the named list of feature
tensors of the batch:

\if{html}{\out{<div class="sourceCode">}}\preformatted{as_task_torch(data, output_dim = ncol(data),
  target_batchgetter = function(data, x) x[[1L]],
  prediction_encoder = function(task, predict_tensor, predict_type) \{
    list(response = as.matrix(predict_tensor$cpu()))
  \})
}\if{html}{\out{</div>}}

Such a task has no \code{truth}, so its measures read the ground truth from the task, which
\code{\link[=msr_torch]{msr_torch()}} arranges for any function that declares a \code{task} argument.
}

\section{Scoring}{
 \verb{$truth()} returns the target column itself if there is exactly one target, a
\code{\link[data.table:data.table]{data.table}} with one column per target if there are several, and
\code{NULL} if the task has no target at all.
\code{\link[=msr_torch]{msr_torch()}} turns a plain R function of \code{truth} and \code{response} into a \code{\link[mlr3:Measure]{Measure}}
that scores such a prediction.

A measure that is passed as \code{measure} becomes the task's default measure, which is what
\verb{$score()} and \verb{$aggregate()} use when they are called without arguments.
}

\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
# multi-label classification: one logical column per label
d = data.frame(x1 = rnorm(50), x2 = rnorm(50))
d$a = d$x1 > 0
d$b = d$x2 > 0
task = as_task_torch(d, target = c("a", "b"), id = "labels")
task
output_dim_for(task)
\dontshow{\}) # examplesIf}
}
\concept{Task}
\section{Super classes}{
\code{\link[mlr3:Task]{mlr3::Task}} -> \code{\link[mlr3:TaskSupervised]{mlr3::TaskSupervised}} -> \code{TaskTorch}
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{target_batchgetter}}{(\verb{function()} or \code{NULL})\cr
See the construction argument.}

    \item{\code{prediction_encoder}}{(\verb{function()} or \code{NULL})\cr
See the construction argument.}

    \item{\code{measure}}{(\code{\link[mlr3:Measure]{Measure}} or \code{NULL})\cr
See the construction argument.}
  }
  \if{html}{\out{</div>}}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{hash}}{(\code{character(1)})\cr
The hash of the task.
In addition to what \code{\link[mlr3:Task]{Task}} hashes, this covers the fields that define the
learning problem, so that two tasks over the same data but with different encodings do not
collide.}

    \item{\code{output_dim}}{(\code{integer(1)})\cr
The number of output units the network needs, see section \emph{Inference}.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-TaskTorch-initialize}{\code{TaskTorch$new()}}
    \item \href{#method-TaskTorch-truth}{\code{TaskTorch$truth()}}
    \item \href{#method-TaskTorch-clone}{\code{TaskTorch$clone()}}
  }
}
\if{html}{\out{<details><summary>Inherited methods</summary>
<ul>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="add_strata"><a href='../../mlr3/html/Task.html#method-Task-add_strata'><code>mlr3::Task$add_strata()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="cbind"><a href='../../mlr3/html/Task.html#method-Task-cbind'><code>mlr3::Task$cbind()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="data"><a href='../../mlr3/html/Task.html#method-Task-data'><code>mlr3::Task$data()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="droplevels"><a href='../../mlr3/html/Task.html#method-Task-droplevels'><code>mlr3::Task$droplevels()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="filter"><a href='../../mlr3/html/Task.html#method-Task-filter'><code>mlr3::Task$filter()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="format"><a href='../../mlr3/html/Task.html#method-Task-format'><code>mlr3::Task$format()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="formula"><a href='../../mlr3/html/Task.html#method-Task-formula'><code>mlr3::Task$formula()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="head"><a href='../../mlr3/html/Task.html#method-Task-head'><code>mlr3::Task$head()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="help"><a href='../../mlr3/html/Task.html#method-Task-help'><code>mlr3::Task$help()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="levels"><a href='../../mlr3/html/Task.html#method-Task-levels'><code>mlr3::Task$levels()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="materialize_view"><a href='../../mlr3/html/Task.html#method-Task-materialize_view'><code>mlr3::Task$materialize_view()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="missings"><a href='../../mlr3/html/Task.html#method-Task-missings'><code>mlr3::Task$missings()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="print"><a href='../../mlr3/html/Task.html#method-Task-print'><code>mlr3::Task$print()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="rbind"><a href='../../mlr3/html/Task.html#method-Task-rbind'><code>mlr3::Task$rbind()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="rename"><a href='../../mlr3/html/Task.html#method-Task-rename'><code>mlr3::Task$rename()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="select"><a href='../../mlr3/html/Task.html#method-Task-select'><code>mlr3::Task$select()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="set_col_roles"><a href='../../mlr3/html/Task.html#method-Task-set_col_roles'><code>mlr3::Task$set_col_roles()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="set_levels"><a href='../../mlr3/html/Task.html#method-Task-set_levels'><code>mlr3::Task$set_levels()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Task" data-id="set_row_roles"><a href='../../mlr3/html/Task.html#method-Task-set_row_roles'><code>mlr3::Task$set_row_roles()</code></a></span></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TaskTorch-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-TaskTorch-initialize}{}}}
\subsection{\code{TaskTorch$new()}}{
  Creates a new instance of this \link[R6:R6Class]{R6} class.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TaskTorch$new(
  id,
  backend,
  target = character(0),
  label = NA_character_,
  target_batchgetter = NULL,
  output_dim = NULL,
  prediction_encoder = NULL,
  measure = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{id}}{(\code{character(1)})\cr
The id of the task.}
      \item{\code{backend}}{(\code{\link[mlr3:DataBackend]{DataBackend}} or \code{data.frame()})\cr
The data.}
      \item{\code{target}}{(\code{character()})\cr
The names of the target columns. May be empty.}
      \item{\code{label}}{(\code{character(1)})\cr
The label of the task.}
      \item{\code{target_batchgetter}}{(\verb{function()} or \code{NULL})\cr
Converts the target columns of a batch into the target tensor \code{y}.
Takes an argument \code{data}, a \code{\link[data.table:data.table]{data.table}} with only the target columns,
and optionally an argument \code{x}, the named list of feature tensors of the batch.
If \code{NULL} (default), it is inferred from the target column types.}
      \item{\code{output_dim}}{(\code{integer(1)} or \code{NULL})\cr
The number of output units the network needs.
If \code{NULL} (default), it is inferred from the target column types.}
      \item{\code{prediction_encoder}}{(\verb{function()} or \code{NULL})\cr
Converts the network output into a prediction.
Takes the arguments \code{task}, \code{predict_tensor} and \code{predict_type} and returns a named \code{list()}
with elements \code{response} and, optionally, \code{prob}.
If \code{NULL} (default), it is inferred from the target column types.}
      \item{\code{measure}}{(\code{\link[mlr3:Measure]{Measure}} or \code{NULL})\cr
The default measure of the task, see section \emph{Scoring}.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TaskTorch-truth"></a>}}
\if{latex}{\out{\hypertarget{method-TaskTorch-truth}{}}}
\subsection{\code{TaskTorch$truth()}}{
  The ground truth, see section \emph{Scoring}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TaskTorch$truth(rows = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{rows}}{(\code{integer()})\cr
The rows to return the truth for. All rows if \code{NULL}.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TaskTorch-clone"></a>}}
\if{latex}{\out{\hypertarget{method-TaskTorch-clone}{}}}
\subsection{\code{TaskTorch$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TaskTorch$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
