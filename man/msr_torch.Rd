% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MeasureTorch.R
\name{msr_torch}
\alias{msr_torch}
\title{Create a Measure for a Generic Torch Task}
\usage{
msr_torch(
  id,
  fun,
  minimize = TRUE,
  range = c(-Inf, Inf),
  predict_type = "response",
  properties = character(),
  label = NA_character_
)
}
\arguments{
\item{id}{(\code{character(1)})\cr
The id of the measure.}

\item{fun}{(\verb{function()})\cr
The scoring function.
It receives whichever of the arguments \code{truth}, \code{response}, \code{prob}, \code{se}, \code{prediction}, \code{task},
\code{learner} and \code{train_set} it declares, and returns a single number.}

\item{minimize}{(\code{logical(1)})\cr
Whether a smaller score is better.}

\item{range}{(\code{numeric(2)})\cr
The range of possible scores.}

\item{predict_type}{(\code{character(1)})\cr
The predict type the measure requires: \code{"response"} (default), \code{"prob"} or \code{"se"}.}

\item{properties}{(\code{character()})\cr
Properties of the measure, see \code{\link[mlr3:Measure]{Measure}}.
The \code{"requires_task"}, \code{"requires_learner"} and \code{"requires_train_set"} properties are added
automatically when \code{fun} declares the corresponding argument.}

\item{label}{(\code{character(1)})\cr
The label of the measure.}
}
\value{
\code{\link{MeasureTorch}}
}
\description{
Short form for constructing a \code{\link{MeasureTorch}}, the quick way to score the predictions of a
\code{\link{TaskTorch}} with a plain R function.
}
\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
# multi-label hamming loss, where truth and response are logical matrices
msr_torch("hamming", function(truth, response) mean(as.matrix(truth) != response))

# a measure that reads the ground truth from the task, which is what a task without a target needs
msr_torch("reconstruction", function(task, prediction) {
  truth = as.matrix(task$data(rows = prediction$row_ids, cols = task$feature_names))
  mean((truth - prediction$response)^2)
}, range = c(0, Inf))
\dontshow{\}) # examplesIf}
}
