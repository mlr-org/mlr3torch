% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/num_embeddings.R
\name{compute_bins}
\alias{compute_bins}
\title{Compute Bin Edges for Piecewise-Linear Embeddings}
\usage{
compute_bins(x, n_bins = 48L)
}
\arguments{
\item{x}{(\code{\link[torch:torch_tensor]{torch_tensor}}, \code{matrix()} or \code{data.frame()})\cr
The training data of the numerical features, of shape \verb{(n, n_features)}.}

\item{n_bins}{(\code{integer(1)})\cr
The number of bins. Must be larger than 1 and smaller than the number of rows of
\code{x}. Default is \code{48}.}
}
\value{
A \code{list()} of one-dimensional \code{\link[torch:torch_tensor]{torch_tensor}}s with the
bin edges of each feature. A feature has at most \code{n_bins + 1} edges (fewer if some
quantiles coincide).
}
\description{
Computes the quantile-based bin edges (Section 3.2.1 of the paper) that
\code{\link[=nn_piecewise_linear_embeddings]{nn_piecewise_linear_embeddings()}} expects.
The bins must be computed on the \strong{training data}.

The tree-based binning of Section 3.2.2 of the paper is not implemented, because it
requires fitting decision trees, which would add a package dependency.
}
\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
bins = compute_bins(matrix(rnorm(200), ncol = 2), n_bins = 4)
lengths(lapply(bins, as.numeric))
\dontshow{\}) # examplesIf}
}
\references{
Gorishniy Y, Rubachev I, Babenko A (2022).
\dQuote{On Embeddings for Numerical Features in Tabular Deep Learning.}
In \emph{Advances in Neural Information Processing Systems 35 (NeurIPS)}.
2203.05556, \url{https://arxiv.org/abs/2203.05556}.
}
