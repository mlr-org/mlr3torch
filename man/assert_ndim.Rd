% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_ndim}
\alias{assert_ndim}
\title{Assert the Number of Dimensions of a Shape}
\usage{
assert_ndim(shape, ndim = NULL, id, min = NULL, max = NULL)
}
\arguments{
\item{shape}{(\code{integer()})\cr
The input shape, with \code{NA} for the dimensions whose size is unknown.}

\item{ndim}{(\code{integer()})\cr
The number(s) of dimensions the operator accepts, the batch dimension included.
Alternatively give \code{min} and/or \code{max}.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.}

\item{min}{(\code{integer(1)})\cr
The smallest number of dimensions the operator accepts, \code{NULL} for no lower bound.}

\item{max}{(\code{integer(1)})\cr
The largest number of dimensions the operator accepts, \code{NULL} for no upper bound.}
}
\value{
The shape, invisibly.
}
\description{
Rejects a shape with the wrong number of dimensions. Give either \code{ndim}, the number(s) of
dimensions the operator accepts, or the bounds \code{min} and \code{max} (either on its own), which is what
an operator that accepts a range of them uses, as batch normalization does.
}
\examples{
assert_ndim(c(NA, 3, 32, 32), 4L, id = "nn_conv2d")
try(assert_ndim(c(NA, 3), 4L, id = "nn_conv2d"))
# batch normalization accepts a range
try(assert_ndim(c(NA, 3, 4, 5), id = "nn_batch_norm1d", min = 2L, max = 3L))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
