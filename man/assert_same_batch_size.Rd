% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_same_batch_size}
\alias{assert_same_batch_size}
\title{Assert that Shapes have the Same Batch Size}
\usage{
assert_same_batch_size(shapes, id)
}
\arguments{
\item{shapes}{(\code{list()} of \code{integer()})\cr
The input shapes.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.}
}
\value{
(\code{integer(1)}) The common batch size, invisibly, or \code{NA_integer_} if no input has a
known one.
}
\description{
Rejects inputs whose batch sizes, i.e. first dimensions, disagree. An unknown (\code{NA}) batch size
is compatible with any other, so only the known ones have to agree.

Unlike the other assertions this returns the common batch size rather than its input, because
that is what the caller needs next: an operator that drops the batch dimension while it works
has to put it back afterwards.
}
\examples{
assert_same_batch_size(list(c(8, 3), c(8, 5)), id = "nn_block")
# an unknown batch size is compatible with a known one, which is the one that is returned
assert_same_batch_size(list(c(NA, 3), c(8, 5)), id = "nn_block")
try(assert_same_batch_size(list(c(8, 3), c(4, 5)), id = "nn_block"))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
