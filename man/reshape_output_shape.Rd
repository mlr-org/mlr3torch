% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{reshape_output_shape}
\alias{reshape_output_shape}
\title{Output Shape of a Reshape}
\usage{
reshape_output_shape(shape_in, shape, id)
}
\arguments{
\item{shape_in}{(\code{integer()})\cr
The input shape, with \code{NA} for the dimensions whose size is unknown.}

\item{shape}{(\code{integer()} | \verb{function()})\cr
The target shape, where \code{-1} marks the dimension that \code{torch} infers from the number of
elements. A \verb{function(shape)} of the input shape is called on it and must return such a vector,
which lets a reshape be expressed for inputs whose sizes are not known in advance, e.g.
\verb{\\(shape) c(shape[1:2], 10)}.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error messages name.}
}
\value{
(\code{integer()}) The output shape.
}
\description{
The shape of \code{\link[torch:torch_reshape]{torch_reshape(x, shape)}}, which is what
\code{\link[=mlr_pipeops_nn_reshape]{nn_reshape()}} infers. The dimension that \code{torch} infers from the
number of elements is resolved here whenever that number is known, and stays unknown otherwise.
}
\examples{
reshape_output_shape(c(NA, 3, 4), c(-1, 12), id = "nn_reshape")
reshape_output_shape(c(NA, 3, 4), \(shape) c(shape[1], 12), id = "nn_reshape")
}
\seealso{
Other Shape Inference:
\code{\link[=infer_shapes]{infer_shapes()}},
\code{\link[=shape_to_str]{shape_to_str()}}
}
\concept{Shape Inference}
