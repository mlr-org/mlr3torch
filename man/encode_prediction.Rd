% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner_torch_methods.R
\name{encode_prediction}
\alias{encode_prediction}
\title{Encode the Network Output as a Prediction}
\usage{
encode_prediction(task, predict_tensor, predict_type, ...)
}
\arguments{
\item{task}{(\code{\link[mlr3:Task]{Task}})\cr
The task to predict on.}

\item{predict_tensor}{(\code{\link[torch:torch_tensor]{torch_tensor}} or \code{list()} of them)\cr
The raw output of the network in evaluation mode.
A network with more than one head -- e.g. one predicting a mean and a standard deviation --
returns a \code{list()} of tensors, which is passed on unchanged.
The encodings of the built-in task types expect a single tensor.}

\item{predict_type}{(\code{character(1)})\cr
The predict type of the learner, e.g. \code{"response"} or \code{"prob"}.}

\item{...}{(any)\cr
Additional arguments. Not used yet.}
}
\value{
named \code{list()}
}
\description{
Converts the raw output of a network into a \code{list()} that can be passed to
\code{\link[mlr3:as_prediction_data]{mlr3::as_prediction_data()}}, which is what the private \code{.encode_prediction()} method of a
\code{\link{LearnerTorch}} has to return.

This is the default implementation that is used by \code{\link{LearnerTorch}} and
\code{\link[=mlr_learners_torch_model]{LearnerTorchModel}}, i.e. by all learners that don't overwrite
\code{.encode_prediction()}.
When adding support for a custom task type, implement a method for the corresponding
\code{\link[mlr3:Task]{Task}} class, which makes the generic torch learners work for that task type.

For the network output that is expected for the built-in task types, see section
\emph{Network Head and Target Encoding} of \code{\link{LearnerTorch}}.
}
