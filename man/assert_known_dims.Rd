% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_known_dims}
\alias{assert_known_dims}
\title{Assert that Dimensions are Known}
\usage{
assert_known_dims(shape, dims, what, id = NULL)
}
\arguments{
\item{shape}{(\code{integer()})\cr
The input shape.}

\item{dims}{(\code{integer()})\cr
Indices of the dimensions that must be known.}

\item{what}{(\code{character(1)})\cr
Describes those dimensions in the error message, e.g. \code{"the channel dimension (dimension 2)"}.}

\item{id}{(\code{character(1)} | \code{NULL})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}} the assertion is made for, which the error
message names. \code{NULL} when the assertion is not made for a \code{PipeOp}.}
}
\value{
The shape, invisibly.
}
\description{
Ensure that a specific dimension is known, i.e. not \code{NA}.
}
\examples{
# a convolution needs the number of input channels, but not the spatial extent
assert_known_dims(c(NA, 3, NA, NA), 2, "the number of channels", id = "nn_conv2d")
try(assert_known_dims(c(NA, NA, 10, 10), 2, "the number of channels", id = "nn_conv2d"))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
