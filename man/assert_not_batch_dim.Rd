% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_not_batch_dim}
\alias{assert_not_batch_dim}
\title{Assert that a Dimension is not the Batch Dimension}
\usage{
assert_not_batch_dim(dim, shape, id)
}
\arguments{
\item{dim}{(\code{integer(1)})\cr
The resolved dimension that the operator changes, see \code{\link[=resolve_dim]{resolve_dim()}}.}

\item{shape}{(\code{integer()})\cr
The input shape, used for the error message.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.}
}
\value{
The dimension, invisibly.
}
\description{
Rejects an operation on the first dimension, which is the batch dimension. An operator that
changes it would silently change the number of observations, which fails much later with a
mismatch against the target.
}
\examples{
assert_not_batch_dim(2L, c(NA, 3, 8), id = "nn_squeeze")
try(assert_not_batch_dim(1L, c(NA, 3, 8), id = "nn_squeeze"))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
