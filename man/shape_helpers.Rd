% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R, R/shape.R
\name{shape_to_str}
\alias{shape_to_str}
\alias{shape_helpers}
\alias{broadcast_shapes}
\alias{resolve_dim}
\title{Helpers for Shape Inference}
\usage{
shape_to_str(x)

broadcast_shapes(shapes, id)

resolve_dim(dim, shape, insert = FALSE)
}
\arguments{
\item{x}{(\code{integer()} | \code{list()} of \code{integer()} | \code{NULL})\cr
The shape(s) to format. \code{NULL} stands for an unknown shape.}

\item{shapes}{(\code{list()} of \code{integer()})\cr
The input shapes, all with the same number of dimensions.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.}

\item{dim}{(\code{integer()})\cr
The dimension(s) the operator addresses. Negative ones count back from the last dimension:
\code{-1} is the last dimension, \code{-2} the one before it, and so on.}

\item{shape}{(\code{integer()})\cr
The input shape.}

\item{insert}{(\code{logical(1)})\cr
Whether a dimension is inserted rather than addressed, as by
\code{\link[=mlr_pipeops_nn_unsqueeze]{nn_unsqueeze()}}: there is then one more position than the shape
has dimensions, \code{-1} appending a new last one.}
}
\value{
\code{broadcast_shapes()} returns an \code{integer()} shape, \code{resolve_dim()} an \code{integer()} of the same
length as \code{dim}, and \code{shape_to_str()} a \code{character(1)}.
}
\description{
Helpers for writing the \code{private$.shapes_out()} method of a \code{\link{PipeOpTorch}}. They all propagate
unknown (\code{NA}) dimensions, see the "Shape Inference" section of \code{\link{PipeOpTorch}} for the shape conventions.
}
\details{
\itemize{
\item \code{broadcast_shapes()} applies the broadcasting rules of \code{torch}, generalized to shapes that may
contain \code{NA}. Per dimension a known size that is not 1 wins; if all known sizes are 1 and some
input is unknown, the result is unknown, because the unknown one may be greater than 1 and
would then determine the size. The shapes must already have the same number of dimensions:
shorter ones are not left-padded with 1s, because the first dimension is the batch dimension.
\item \code{resolve_dim()} resolves dimension indices that count from the end, as in \code{torch}, to positive
ones. Indices that are out of range stay out of range, so that
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}} reports them.
\item \code{shape_to_str()} formats a shape, or a \code{list()} of them, for an error message.
}
}
\examples{
broadcast_shapes(list(c(NA, 1), c(NA, 6)), id = "nn_merge_sum")
resolve_dim(-1, c(NA, 3, 8))
shape_to_str(c(NA, 3, 8))
}
\seealso{
Other Shape Inference:
\code{\link[=infer_shapes]{infer_shapes()}},
\code{\link[=reshape_output_shape]{reshape_output_shape()}}
}
\concept{Shape Inference}
