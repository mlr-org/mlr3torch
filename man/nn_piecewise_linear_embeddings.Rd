% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/num_embeddings.R
\name{nn_piecewise_linear_embeddings}
\alias{nn_piecewise_linear_embeddings}
\title{Piecewise-Linear Embeddings for Numerical Features}
\usage{
nn_piecewise_linear_embeddings(bins, d_embedding, activation, version)
}
\arguments{
\item{bins}{(\code{list()} of \code{\link[torch:torch_tensor]{torch_tensor}})\cr
The bin edges, as computed by \code{\link[=compute_bins]{compute_bins()}} on the training data.}

\item{d_embedding}{(\code{integer(1)})\cr
The embedding size.}

\item{activation}{(\code{logical(1)})\cr
Whether to apply a ReLU activation in the end.}

\item{version}{(\code{character(1)})\cr
Either \code{"A"} (the version of the original paper) or \code{"B"} (introduced by the TabM
paper; adds a \code{\link[=nn_linear_embeddings]{nn_linear_embeddings()}} shortcut and zero-initializes the
piecewise-linear part, so that the module behaves like a linear embedding at
initialization). \code{"B"} is required by \code{\link[=nn_tabm]{nn_tabm()}}.
Unlike upstream, this argument must be given explicitly.}
}
\description{
The piecewise-linear embeddings of the paper: each numerical feature is first encoded
by the piecewise-linear encoding defined by its bin edges (see \code{\link[=compute_bins]{compute_bins()}}) and
then embedded with a per-feature linear layer.
For an input of shape \verb{(batch, n_features)} the output shape is
\verb{(batch, n_features, d_embedding)}.
}
\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
bins = compute_bins(matrix(rnorm(200), ncol = 2), n_bins = 4)
m = nn_piecewise_linear_embeddings(bins, d_embedding = 8, activation = FALSE,
  version = "B")
m(torch::torch_randn(3, 2))$shape
\dontshow{\}) # examplesIf}
}
\references{
Gorishniy Y, Rubachev I, Babenko A (2022).
\dQuote{On Embeddings for Numerical Features in Tabular Deep Learning.}
In \emph{Advances in Neural Information Processing Systems 35 (NeurIPS)}.
2203.05556, \url{https://arxiv.org/abs/2203.05556}.

Gorishniy Y, Kotelnikov A, Babenko A (2025).
\dQuote{TabM: Advancing Tabular Deep Learning with Parameter-Efficient Ensembling.}
In \emph{The Thirteenth International Conference on Learning Representations (ICLR)}.
2410.24210, \url{https://openreview.net/forum?id=Sd4wYYOhmY}.
}
