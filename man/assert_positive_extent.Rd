% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_positive_extent}
\alias{assert_positive_extent}
\title{Assert that a Computed Output Size is Positive}
\usage{
assert_positive_extent(extent, shape_in, id)
}
\arguments{
\item{extent}{(\code{numeric()})\cr
The sizes an operator computed for the dimensions it changes, e.g. the spatial dimensions of a
convolution.}

\item{shape_in}{(\code{integer()})\cr
The input shape, used for the error message.}

\item{id}{(\code{character(1)} | \code{NULL})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.
\code{NULL} when the assertion is not made for a \code{PipeOp}.}
}
\value{
The extent, invisibly.
}
\description{
Rejects a computed output size that no tensor can have, i.e. one that is not a positive number.
Operators such as convolutions and pooling compute their spatial output sizes from \code{kernel_size},
\code{stride}, \code{padding} and \code{dilation}, a combination of which can produce a size of zero or less.
Unknown (\code{NA}) sizes are accepted, since nothing can be said about them.
}
\examples{
assert_positive_extent(c(30, NA), c(NA, 3, 32, 32), id = "nn_conv2d")
try(assert_positive_extent(c(0, 4), c(NA, 3, 32, 32), id = "nn_conv2d"))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
