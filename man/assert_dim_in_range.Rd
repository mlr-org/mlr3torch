% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_dim_in_range}
\alias{assert_dim_in_range}
\title{Assert that a Dimension Exists}
\usage{
assert_dim_in_range(dim, true_dim, shape, id)
}
\arguments{
\item{dim}{(\code{integer(1)})\cr
The dimension as the user specified it: it may count back from the last dimension. Only used
for the error message.}

\item{true_dim}{(\code{integer(1)})\cr
The same dimension resolved to a positive index, see \code{\link[=resolve_dim]{resolve_dim()}}.}

\item{shape}{(\code{integer()})\cr
The shape that \code{true_dim} addresses.}

\item{id}{(\code{character(1)})\cr
The id of the \code{\link[mlr3pipelines:PipeOp]{PipeOp}}, which the error message names.}
}
\value{
The resolved dimension, invisibly.
}
\description{
Rejects a \code{dim} parameter that does not address a dimension of \code{shape}. Resolve negative indices
with \code{\link[=resolve_dim]{resolve_dim()}} first and pass both, so that the error message can report the value the
user actually specified.
}
\examples{
shape = c(NA, 3, 8, 8)
assert_dim_in_range(-1L, resolve_dim(-1L, shape), shape, id = "nn_squeeze")
try(assert_dim_in_range(-5L, resolve_dim(-5L, shape), shape, id = "nn_squeeze"))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}},
\code{\link[=assert_shapes]{assert_shapes()}}
}
\concept{Shape Assertions}
