% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerTorchTabM.R
\name{nn_tabm_loss}
\alias{nn_tabm_loss}
\title{Ensemble Loss Adapter for TabM}
\usage{
nn_tabm_loss(loss)
}
\arguments{
\item{loss}{(\code{\link[torch:nn_module]{nn_module}})\cr
The loss module that is applied to the folded prediction and the repeated target.}
}
\description{
Adapts a loss function to the \verb{(batch, k, d_out)} output of \code{\link[=nn_tabm]{nn_tabm()}}:
the ensemble dimension is folded into the batch dimension and every target is repeated
\code{k} times, so that all \code{k} submodels are trained on the full batch.
This is what the reference implementation of TabM does.

\code{\link{LearnerTorchTabM}} applies this adapter automatically to whatever loss it is
configured with; it only needs to be used explicitly when combining \code{\link[=nn_tabm]{nn_tabm()}} with
\code{\link{LearnerTorchModule}}.
}
\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
loss = nn_tabm_loss(torch::nn_cross_entropy_loss())
input = torch::torch_randn(6, 4, 3)
target = torch::torch_randint(1, 3, 6, dtype = torch::torch_long())
loss(input, target)
\dontshow{\}) # examplesIf}
}
\references{
Gorishniy Y, Kotelnikov A, Babenko A (2025).
\dQuote{TabM: Advancing Tabular Deep Learning with Parameter-Efficient Ensembling.}
In \emph{The Thirteenth International Conference on Learning Representations (ICLR)}.
2410.24210, \url{https://openreview.net/forum?id=Sd4wYYOhmY}.
}
