% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/num_embeddings.R
\name{nn_periodic_embeddings}
\alias{nn_periodic_embeddings}
\title{Periodic Embeddings for Numerical Features}
\usage{
nn_periodic_embeddings(
  n_features,
  d_embedding = 24L,
  n_frequencies = 48L,
  frequency_init_scale = 0.01,
  activation = TRUE,
  lite
)
}
\arguments{
\item{n_features}{(\code{integer(1)})\cr
The number of numerical features.}

\item{d_embedding}{(\code{integer(1)})\cr
The embedding size. Default is \code{24}.}

\item{n_frequencies}{(\code{integer(1)})\cr
The number of frequencies of each feature (\code{k} in Section 3.3 of the paper).
Default is \code{48}.}

\item{frequency_init_scale}{(\code{numeric(1)})\cr
The initialization scale of the frequencies (\code{sigma} in Section 3.3 of the paper).
This is an important hyperparameter. Default is \code{0.01}.}

\item{activation}{(\code{logical(1)})\cr
Whether to apply the ReLU activation. Must be \code{TRUE} if \code{lite} is \code{TRUE}.
Default is \code{TRUE}.}

\item{lite}{(\code{logical(1)})\cr
If \code{TRUE}, the outer linear layer is shared between all features (the variant
introduced by the TabR paper). Has no default upstream.}
}
\description{
Embeddings for numerical features based on periodic activations, i.e. the \emph{PLR}
embeddings of the paper: each feature is passed through \code{cos}/\code{sin} of \code{n_frequencies}
learned frequencies, followed by a linear layer and (optionally) a ReLU.
For an input of shape \verb{(*, n_features)} the output shape is
\verb{(*, n_features, d_embedding)}.
}
\examples{
\dontshow{if (torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
m = nn_periodic_embeddings(3, 8, lite = FALSE)
m(torch::torch_randn(2, 3))$shape
\dontshow{\}) # examplesIf}
}
\references{
Gorishniy Y, Rubachev I, Babenko A (2022).
\dQuote{On Embeddings for Numerical Features in Tabular Deep Learning.}
In \emph{Advances in Neural Information Processing Systems 35 (NeurIPS)}.
2203.05556, \url{https://arxiv.org/abs/2203.05556}.
}
