% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{assert_shapes}
\alias{assert_shapes}
\title{Assert a List of Shapes}
\usage{
assert_shapes(
  shapes,
  coerce = TRUE,
  named = FALSE,
  null_ok = FALSE,
  unknown_batch = NULL
)
}
\arguments{
\item{shapes}{(\code{list()} of \code{integer()})\cr
A \code{list()} of shapes.}

\item{coerce}{(\code{logical(1)})\cr
Whether to coerce the shapes to \code{integer()} if possible.}

\item{named}{(\code{logical(1)})\cr
Whether the shapes must be uniquely named.}

\item{null_ok}{(\code{logical(1)})\cr
Whether \code{NULL}, i.e. a wholly unknown shape, is valid.}

\item{unknown_batch}{(\code{logical(1)} | \code{NULL})\cr
Whether the batch dimension \strong{must} be unknown, i.e. \code{NA}.
If left \code{NULL} (default), the first dimension can be \code{NA} or not.}
}
\value{
The shapes, coerced to \code{integer()} if \code{coerce} is \code{TRUE}.
}
\description{
Checks that \code{shapes} is a non-empty \code{list()} of valid shapes, see \code{\link[=assert_shape]{assert_shape()}}.
}
\examples{
assert_shapes(list(c(NA, 3), c(NA, 5)))
}
\seealso{
Other Shape Assertions:
\code{\link[=assert_dim_in_range]{assert_dim_in_range()}},
\code{\link[=assert_known_dims]{assert_known_dims()}},
\code{\link[=assert_ndim]{assert_ndim()}},
\code{\link[=assert_not_batch_dim]{assert_not_batch_dim()}},
\code{\link[=assert_positive_extent]{assert_positive_extent()}},
\code{\link[=assert_same_batch_size]{assert_same_batch_size()}},
\code{\link[=assert_same_ndim]{assert_same_ndim()}},
\code{\link[=assert_shape]{assert_shape()}}
}
\concept{Shape Assertions}
