#library(torch)

# ns = getNamespace("torch")
# idxs = grep("optim", names(ns))
# names = names(ns)[idxs]
# f = function(x) get(x, env = ns)
# tops = map(names, f)
#
# params = unlist(map(tops, formalArgs))
#
#
#
# get(names(ns)[grep("optim", names(ns))], env = ns)
