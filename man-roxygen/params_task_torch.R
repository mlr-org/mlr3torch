#' @param target (`character()` or `NULL`)\cr
#'   The names of the target columns.
#'   `NULL` (default) for a task without a target, see section *Tasks without a Target* of
#'   [`TaskTorch`].
#' @param id (`character(1)`)\cr
#'   The id of the task.
